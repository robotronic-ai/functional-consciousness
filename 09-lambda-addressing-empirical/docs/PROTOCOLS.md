# Canonical Protocols

## P1. Real-C0 nonderivability screen

Candidate selection was based only on the frozen baseline screening representation. Lambda descriptors and task outcomes were sealed. Stage A could nominate candidates but could not establish the scientific claim. Stage B would have opened Lambda only for frozen Stage-A candidates.

No candidate was found, so Stage B did not exist.

## P2. Rich-Delta nonreduction screen

The screening object was the full typed four-probe perturbation-response channel, not a scalar Delta summary.

Each context group contained ordered probes `0,1,2,3`. Responses concatenated:

- post-write memory: `8 x 768`,
- post-filler memory: `8 x 768`,
- query classifier input: `768`.

Per probe raw dimension: `13,056`.
Full four-probe rich dimension: `52,224`.

Equality required every component of every ordered probe at all three roles to lie within the frozen tolerance. No averaging, pairwise rescue, role dropping, or Lambda/task-outcome based selection was permitted.

## P3. Layer-complete extension

The rich-Delta protocol was held fixed while the structural head-swap family was extended across all 12 layers.

No layer, head, lineage, or candidate was selected after observing Lambda.

## P4. Constructive late-role-rebind witness

The synthetic witness deliberately isolates one causal edge.

Baseline comparisons disable COMMIT and exhaustively compare all declared permutations and internal swap interventions.

The challenge then activates COMMIT after a late routing cue. The operational witness is

\[
\hat\Lambda_{gate}=I(U;P_{after})/H(U).
\]

Expected exact values:

- RELINK = `1`,
- LOCK = `0`.

Controls require both systems to succeed when the routing is identity, pre-cued, or externally forced.
