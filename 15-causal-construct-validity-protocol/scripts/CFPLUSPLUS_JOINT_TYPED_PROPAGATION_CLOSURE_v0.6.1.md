# CF++ JOINT TYPED PROPAGATION — Closure v0.6.1

**Date:** 2026-09-14

## Status

The initial v0.6 harness returned `REVIEW` only because the frozen cross-type
swap control used a channel that was exactly symmetric under that swap.

A candidate-fixed asymmetric holdout was then frozen and executed without
modifying the candidate.

Holdout verdict:

\[
\boxed{
\texttt{CFPLUSPLUS-CROSS-TYPE-HOLDOUT-PASS}
}
\]

Therefore the v0.6 candidate is retained.

## Retained profile candidate

\[
\boxed{
C_F^{++}
=
[
(
\Gamma,
\mathcal D_q^{++},
R(k)
)
]
}
\]

with:

\[
\mathcal D_q^{++}
=
(
\Delta_{\rm prop},
[K^{\rm joint}_{q,\mathcal R}]_{\rm adm}
).
\]

The joint typed response channel repairs both previously frozen insufficiency
witnesses:

1. typed content-to-role binding;
2. cross-role synergistic content.

The scalar \(\Delta_{\rm prop}\) is unchanged and remains the scalar gate.

## Exact theorem retained

At fixed source distribution, target semantics, loss, and admissible decision
rules, equality of the typed joint response channel implies equality of the
optimal achievable expected loss for every **static** downstream access task
that reads only the certified joint response.

This is a sufficiency statement about the declared static response channel,
not about consciousness phenomenology.

## Remaining open scope

The current object is not a trajectory process.

It does not yet establish sufficiency for:

- delayed access;
- corrective updating across time;
- temporal order;
- path-specific gating;
- state-transition binding.

These are the next adversarial targets.
